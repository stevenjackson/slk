# Slack user token (xoxp-...). Required.
# Create at https://api.slack.com/apps — see README for required OAuth scopes.
SLACK_USER_TOKEN=xoxp-your-token-here

# Path to SQLite database. Optional, defaults to ~/.slk/slk.db
# SLK_DB=/path/to/slk.db
